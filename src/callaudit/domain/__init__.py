"""Domain core: rubric, verdicts, score and report.

Nothing in this package imports a web framework, a database driver or a
language model SDK. Those live behind ports in `callaudit.application.ports`.
"""
